# E7 — 2026-08-30

- Fixture repository: `agent-brains`
- Fixture commit: `e2670b7`
- Client/runtime: Pi (version not captured in the final output)
- Provider/model: not captured in the final output
- Candidate: `@modelcontextprotocol/sdk`
- Duration: approximately 8m25s
- Raw report: `/private/tmp/agent-brains-w0-e7/docs/research/mcp-sdk-adoption.md`

## Input

```text
Use the research skill to evaluate whether this repository should adopt
@modelcontextprotocol/sdk. Start from the official MCP documentation and the
official SDK repository, then inspect this repository’s package.json, README,
architecture and relevant source files. Produce one Markdown report with
verified upstream facts and links, local evidence, clearly labelled inferences,
comparison with the current implementation and realistic alternatives, and a
final verdict of adopt, trial, selectively import or reject. Motivate the
verdict using new value, overlap, cost, risk, compatibility and reversibility.
Do not install the dependency, modify source files or commit anything.
```

## Outcome

- Result: report produced with verdict `reject` for adoption today
- Artifacts produced: one Markdown report in the existing `docs/research/` convention
- Constraint violations: none observed
- Dependency installation: none
- Source changes: none
- Commits: none

## Evidence

The session report is 287 lines and contains sections for the verdict,
verified upstream facts, local evidence, inferences, comparison, alternatives
and the six requested decision dimensions. It cites official MCP/SDK sources
and recommends revisiting the decision at Phase 2, considering
`pi-mcp-adapter` first and `@modelcontextprotocol/client` v2 rather than the
named v1 package.

Final worktree verification:

```text
?? docs/research/mcp-sdk-adoption.md
```

The worktree had no tracked diff; only the new report was untracked. The report
path matches the repository's established research-document convention.

- Expected behavior observed: primary-source research, repository comparison, labelled reasoning and an explicit reversible verdict.
- Failure modes observed: none in the captured run.
- Human spot-check: the user supplied the complete Pi transcript; the report structure and worktree state were independently checked.

## Notes

The candidate was not present in the repository's package manifest or the
checked research files before this run. The full report remains in the E7
worktree as the raw research artifact; this file records the evaluation result.
