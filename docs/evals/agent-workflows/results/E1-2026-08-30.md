# E1 — 2026-08-30

- Fixture repository: `agent-brains`
- Fixture commit: `e2670b7`
- Client/runtime: Pi `0.84.3`
- Provider/model: requested `zai` / `glm-5.2`, high thinking; raw responses report `glm-5.3`
- Configuration: `PI_OFFLINE=1`; project `.pi/pi-brain.json` (`stack=nodejs`, verification `npm run typecheck` and `npm test`)
- Raw session: [E1-2026-08-30.jsonl](raw/E1-2026-08-30.jsonl)

## Input

```text
In README.md, rename the heading ## Structure to ## Repository structure.
Make only this one-line documentation change. Do not create a feature
specification or modify source code.
```

## Outcome

- Result: expected one-line documentation edit
- Turns: 1 task turn
- Tool calls: 2 agent tool calls (`read`, `edit`) in the raw session; the post-session `git status` and `git diff --stat` checks were run outside Pi
- Artifacts produced: `README.md` only
- Constraint violations: none observed

## Evidence

The session ran in the disposable fixture `/private/tmp/agent-brains-w0-e1`.
The raw export records the requested prompt, provider/model, thinking level,
and the two agent tool calls.

The final diff was:

```text
README.md | 2 +-
-## Structure
+## Repository structure
```

Independent verification after the session confirmed:

```text
* HEAD (no branch)
 M README.md
README.md | 2 +-
```

- Expected behavior observed: direct edit, no feature specification, no source changes, no subagent or broad verification.
- Failure modes observed: none in the captured session.
- Human spot-check: the root and final diff were inspected in the W0 session.

## Notes

The Pi startup displayed skill-collision and update-available notices. The
project copy was selected for each collision; Pi was not updated during the
run to preserve the baseline runtime. The runtime version is recorded in the
companion configuration snapshot; the W0 reproducibility gate still requires
the corresponding E3 and E7 raw exports and snapshots.
