# agents-skills-custom repo files
.DS_Store

# node package build output (this repo is a node package)
node_modules/
dist/
*.tsbuildinfo

# local pi runtime plumbing (tracked .pi/*.json config stays visible)
.pi/pi-brain.lock
.pi/pi-brain/
# per-skill symlinks into .agents/skills/, created by `npx skills update` for Pi discovery
.pi/skills/
