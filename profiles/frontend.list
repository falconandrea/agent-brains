# Frontend (alias — everything UI). For finer control, profiles can @include the
# subgroups directly: frontend-core / frontend-design / frontend-motion / frontend-qa.
# NOT inherited by nodejs (pure backend / API profile).
@include common
@include frontend-core
@include frontend-design
@include frontend-motion
@include frontend-qa
