<!-- pi-brain:feature "/flow resume <runId>: riprendere una run /feature interrotta senza ripartire da zero. Oggi un'interruzione (Ctrl-C, crash, quota reviewer esaurita — due incidenti reali) perde tutto: alla rilanciata il planner rifà domande e piano da capo. Fonte primaria: docs/pi-brain/IDEAS.md sezione \"Resume mode for interrupted runs\" (design sketch) e src/run-log.ts (log run-state persistito per runId). Implementa /flow resume <runId>: (1) replay dell'event log (.pi/pi-brain/runs/<runId>.jsonl) per derivare l'ultima fase completata e il review round; (2) re-validazione delle preconditions: lock acquisibile, file PRD/tasks presenti con marker pi-brain, baseline git ancora risolvibile; (3) rientro alla prima fase incompleta — se interrotto durante review, il reviewer riparte sul diff attuale senza replay; se il diff è cambiato dall'interruzione (edit umani), warning e scelta restart-vs-resume via ask_user. Due gap noti da chiudere nel design: l'event log NON registra l'approvazione del gate né il baseline commit usato da diffSince — oggi derivabili/irrecuperabili: l'approvazione del gate è inferibile deterministicamente (develop phase.started implica gate approvato), il baseline va persistito (es. campo opzionale in workflow.started, retrocompatibile). La logica di resume deve essere pi-free in src/ (workflows/ o nuovo modulo resume.ts), unit-testata con i fake esistenti come il resto; extensions/pi-brain.ts solo wiring del comando. Fuori scope: cambiare le semantiche del workflow feature (es. inoltrare i warning al fix round — idea separata in IDEAS.md), auto-resume dopo crash, resume di run di altri workflow. Done: npm run typecheck e npm test verdi; test che coprono replay→resume in develop, in review, run con log corrotto/monco, gate non ancora approvato, diff cambiato → ask_user." -->
## TASKS
- **T1 — Event schema extensions** (`src/ports.ts`, `tests/run-log.test.ts`): add optional `description`/`stack`/`baseline` to `workflow.started`, optional `patchSha` to `phase.started`, richer optional issue fields on `review.completed`, and the `workflow.resumed` event; round-trip test.
- **T2 — Persist resume inputs in feature.ts** (`src/workflows/feature.ts`): move `git.baseline()` and stack resolution before the `workflow.started` emission and include the new fields; emit `patchSha` (sha256 of the patch) on review `phase.started`; emit full issue detail on `review.completed`; existing tests stay green.
- **T3 — Mid-loop re-entry refactor** (`src/workflows/feature.ts`): extract developer/verify/review turn helpers, add the optional `ResumeState` parameter (entry point, seeded `reviewRound`/`verifyRetries`/`developerRuns`/`fixes`/`previousBlockingIds`/`lastReview`/seeded usage), export `ResumeState` and `descriptionMarker`; no behaviour change without resume.
- **T4 — Replay module** (`src/workflows/resume.ts`): pure `replayRunLog(events)` deriving gate approval, loop counters, fixes, last review, usage, interrupted `patchSha`; `isResumable(events)` with the decided terminal-state policy (only no-outcome and `workflow.failed` runs).
- **T5 — Resume orchestration** (`src/workflows/resume.ts`): precondition validation (PRD/tasks marker check, baseline resolvable via `diffSince`), changed-diff sha comparison with the restart-vs-resume `select` (Abort = clean stop), `workflow.resumed` emission, delegation into `runFeatureWorkflow` with the resume state.
- **T6 — Acceptance tests** (`tests/resume.test.ts`): full matrix — resume mid-develop, post-develop (verify only), mid-review (round + budget continuity, no planner call), corrupt/torn/missing log, gate not approved, changed diff ⇒ both dialog choices, unchanged diff ⇒ no dialog, old-format/no-baseline log, terminal outcomes, missing artifacts, usage folding, chained resume.
- **T7 — Extension wiring** (`extensions/pi-brain.ts`): `/flow resume <runId>` subcommand (guards, replay, lock with existing refusal texts, port wiring, RunLog append, outcome rendering), `getArgumentCompletions` for resumable ids, `describe()` support for `workflow.resumed`, `ActiveRun`/`/flow stop` integration.
- **T8 — Documentation** (`docs/pi-brain/ARCHITECTURE.md`, `docs/pi-brain/IDEAS.md`, `README.md`): document the resume flow, persisted fields and refusal semantics; close the "Resume UX still missing" gap note; mark the IDEAS.md entry implemented; update the command table.